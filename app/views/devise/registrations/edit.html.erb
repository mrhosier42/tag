<%# <%= link_to "Back", :back %>

<br>
<div class="container">
  <div class="row">
    <div class="col-8 bg-color1 text-white justify-content-center align-items-center pb-4 pt-4 mr-3">
      <div class="d-flex flex-column">
        <h2>Edit <%= resource_name.to_s.humanize %> Account</h2>

        <%= bootstrap_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
          <%= render "devise/shared/error_messages", resource: resource %>

          <div class="field">
            <h5><i>To change your email address, please enter a new one.</i><br/><h5>
            <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
          </div>

          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
          <% end %>

          <div class="field">
            <br>
            <h5><i>To change your password, please enter a new one and re-enter to confirm.</i></h5>
            <%= f.password_field :password, autocomplete: "new-password" %>
            <% if @minimum_password_length %>
              (<em><%= @minimum_password_length %> characters minimum</em>)
            <% end %>
          </div>

          <div class="field">
            <br>
            <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
          </div>

          <div class="field">
            <br>
            <h5><i>To save your changes, enter your existing password.</i></h5>
            <%= f.password_field :current_password, autocomplete: "current-password" %>
          </div>

          <div class="actions">
            <br>
            <%= f.submit "Update", class:"btn btn-secondary btn-lg" %>
          </div>
        <% end %>
      </div>
    </div>

<!--    TODO: need a placeholder, this code was removed-->
<!--    <div class="col-3 bg-color1 text-white justify-content-center align-items-center">-->
<!--      <div>-->
<!--        <div>-->
<!--          <h1>Delete Account</h1>-->
<!--        </div>-->
<!--        <div>-->
          <%#= button_to "Delete Semester", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete, class:"btn btn-secondary btn-lg" %>
<!--        </div>-->
<!--      </div>-->
<!--      <div>-->
<!--        <div>-->
          <%# Will be used to delete a semester and all it's data %>
<!--          <h1>Clear Semester</h1>-->
<!--        </div>-->
<!--        <div>-->
          <%# Delete semester button goes here %>
<!--        </div>-->
<!--      </div>-->
<!--    </div>-->
  </div>
</div>